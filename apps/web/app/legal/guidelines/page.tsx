import { LegalPlaceholder } from '../../../components/legal-placeholder';

export const metadata = { title: 'Panduan Komunitas — CURHAT DONG' };

export default function GuidelinesPage() {
  return <LegalPlaceholder title="Panduan Komunitas" />;
}
