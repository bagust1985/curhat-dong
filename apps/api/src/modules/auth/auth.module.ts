import { Module } from '@nestjs/common';

/**
 * Email OTP, Google OAuth, JWT, rotating refresh (E03)
 *
 * Scaffolded in E01-T05. Controllers and providers land in the epic noted above.
 */
@Module({})
export class AuthModule {}
