# Keep the dependency graph strict: a package must declare what it imports.
# Prevents "works locally, breaks in Docker" phantom-dependency bugs.
node-linker=isolated
strict-peer-dependencies=false

# Never resolve a floating range at install time in CI — the lockfile decides.
prefer-frozen-lockfile=true
